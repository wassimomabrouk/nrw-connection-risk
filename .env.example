# Copy to .env (same folder) and fill in. .env is git-ignored and must never be committed.
DB_CLIENT_ID=your-client-id
DB_API_KEY=your-api-key
# Optional: healthchecks.io ping URLs (collector heartbeat, daily backup)
HEALTHCHECK_URL=
BACKUP_HEALTHCHECK_URL=
